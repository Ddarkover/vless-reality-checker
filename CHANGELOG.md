# VLESS+Reality Camouflage Site Checker

## Release Notes

---

### v1.1.0

#### 🆕 Новое
- **OCSP Stapling** — полностью рабочая проверка через прямой HTTP-запрос к OCSP-серверу (библиотека `cryptography`). Скрипт получает сертификат сервера, извлекает URL OCSP-сервера из AIA-расширения, скачивает issuer-сертификат и отправляет OCSP-запрос напрямую. Именно так работают браузеры и профессиональные SSL-сканеры.
- **Цикл проверок** — после завершения скрипт предлагает меню:
  ```
  1 — Проверить новые домены
  2 — Выход
  ```
  Можно проверять домены неограниченное количество раз без перезапуска.
- **www ↔ без www** — редирект между `domain.com` и `www.domain.com` теперь отображается как предупреждение ⚠️ (жёлтый), а не ошибка ❌. Начисляется 0.5 балла вместо 0.

#### 🛠 Исправления
- **Стабильность** — каждая проверка обёрнута в независимый `try/except`. Ошибка в одной проверке не прерывает остальные и не роняет скрипт.
- **HTTP/2 StreamReset** — если сервер сбрасывает HTTP/2 соединение (`error_code:2 CANCEL`), скрипт автоматически повторяет запрос через HTTP/1.1. Решает проблему с рядом CDN-серверов (Akamai и др.).
- **Читаемые ошибки** — технические сообщения заменены на понятный текст:
  - `_ssl.c:993: The handshake operation timed out` → `Таймаут TLS-рукопожатия`
  - `<StreamReset stream_id:1, error_code:2>` → `Сервер сбросил соединение (StreamReset)`
  - `TLSV1_UNRECOGNIZED_NAME` → `Сервер не поддерживает SNI для этого домена`
- **Верификация сертификатов на Windows** — заменили системное хранилище на `certifi`. Устранена ошибка `CERTIFICATE_VERIFY_FAILED` в PyInstaller-сборке.

---

### v1.0.0 — первый релиз

#### Проверяемые критерии
| # | Критерий |
|---|----------|
| 1 | Сервер находится вне РФ |
| 2 | Поддержка TLS 1.3 и HTTP/2 |
| 3 | Главная страница не редиректит на другой домен |
| 4 | Географическое расстояние до вашего VPN-сервера |
| 5 | Шифрование сообщений после Server Hello (TLS 1.3 / RFC 8446) |
| 6 | OCSP Stapling |

#### Особенности
- Запуск без установки Python — готовый `checker.exe` в разделе Releases
- Поддержка нескольких доменов за один запуск с итоговой сводной таблицей
- Передача доменов через аргументы командной строки: `checker.exe google.com github.com`
- Автоматическая сборка EXE через GitHub Actions при создании тега
